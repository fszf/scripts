#!/usr/bin/env sh
# Script to start dwm in loop

while true; do
		$HOME/src/scripts/dwm-statusbar
    	sleep 2
        #slstatus
done &

while true; do
	# dwm >/dev/null
	# to log stderrors to a file
	dwm 2> ~/dwm.log
done

